{% extends 'base.html.twig' %}

{% block title %}GovernmentParties{% endblock %}

{% block body %}
    <h1>GovernmentParties</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ government_party.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ government_party.name }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ government_party.abbreviation }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ government_party.logo }}</td>
            </tr>
        </tbody>
    </table>

    <h2>DeputiesList</h2>

    {% include 'deputies/list.html.twig' %}

    <a href="{{ path('government_parties_index') }}">back to list</a>

    <a href="{{ path('government_parties_edit', {'id': government_party.id}) }}">edit</a>

    {{ include('government_parties/_delete_form.html.twig') }}
{% endblock %}
