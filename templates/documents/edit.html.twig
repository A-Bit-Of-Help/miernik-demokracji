{% extends 'base.html.twig' %}

{% block title %}{% trans %}documents.edit.site.title{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}documents.edit.title{% endtrans %}</h1>

    {{ include('documents/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('documents_index') }}">
        {% trans %}documents.edit.button.back{% endtrans %}
    </a>

    {{ include('documents/_delete_form.html.twig') }}
{% endblock %}
