<form method="post" action="{{ path('government_meetings_date_delete', {'id': government_meetings_date.id}) }}"
      onsubmit="return confirm('{% trans %}government_meetings_date.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ government_meetings_date.id) }}">
    <button class="btn btn-danger">{% trans %}government_meetings_date.delete.button{% endtrans %}</button>
</form>
