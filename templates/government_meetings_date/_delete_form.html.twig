<form method="post" action="{{ path('government_meetings_date_delete', {'id': government_meetings_date.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ government_meetings_date.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
