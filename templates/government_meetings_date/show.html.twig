{% extends 'base.html.twig' %}

{% block title %}GovernmentMeetingsDate{% endblock %}

{% block body %}
    <h1>GovernmentMeetingsDate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ government_meetings_date.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ government_meetings_date.date ? government_meetings_date.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('government_meetings_date_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('government_meetings_date_edit', {'id': government_meetings_date.id}) }}">edit</a>

    {{ include('government_meetings_date/_delete_form.html.twig') }}
{% endblock %}
