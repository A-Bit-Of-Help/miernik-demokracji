{% extends 'base.html.twig' %}

{% block title %}Edit GovernmentMeetingsDate{% endblock %}

{% block body %}
    <h1>Edit GovernmentMeetingsDate</h1>

    {{ include('government_meetings_date/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('government_meetings_date_index') }}">back to list</a>

    {{ include('government_meetings_date/_delete_form.html.twig') }}
{% endblock %}
