{% extends 'base.html.twig' %}

{% block title %}Edit Deputies{% endblock %}

{% block body %}
    <h1>Edit Deputies</h1>

    {{ include('deputies/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('deputies_index') }}">back to list</a>

    {{ include('deputies/_delete_form.html.twig') }}
{% endblock %}
