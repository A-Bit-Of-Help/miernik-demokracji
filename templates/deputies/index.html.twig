{% extends 'base.html.twig' %}

{% block title %}{% trans %}deputies.index.site.title{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}deputies.index.title{% endtrans %}</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-success" href="{{ path('deputies_new') }}">{% trans %}deputies.index.button.new{% endtrans %}</a>
    {% endif %}

    {% include 'deputies/list.html.twig' %}
{% endblock %}
