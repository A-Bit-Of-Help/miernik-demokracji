<form method="post" action="{{ path('deputies_delete', {'id': deputy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deputy.id) }}">
    <button class="btn">Delete</button>
</form>
