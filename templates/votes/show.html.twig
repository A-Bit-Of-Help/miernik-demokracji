{% extends 'base.html.twig' %}

{% block title %}Votes{% endblock %}

{% block body %}
    <h1>Votes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vote.id }}</td>
            </tr>
            <tr>
                <th>Hour</th>
                <td>{{ vote.hour ? vote.hour|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ vote.title }}</td>
            </tr>
            <tr>
                <th>AgendaItem</th>
                <td>{{ vote.agendaItem }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('votes_index') }}">back to list</a>

    <a href="{{ path('votes_edit', {'id': vote.id}) }}">edit</a>

    {{ include('votes/_delete_form.html.twig') }}
{% endblock %}
