{% extends 'base.html.twig' %}

{% block title %}{% trans %}votes.edit.site.title{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}votes.edit.title{% endtrans %}</h1>

    {{ include('votes/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('votes_index') }}">
        {% trans %}votes.edit.button.back{% endtrans %}
    </a>

    {{ include('votes/_delete_form.html.twig') }}
{% endblock %}
